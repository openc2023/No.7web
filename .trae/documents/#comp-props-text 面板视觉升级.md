## 1. CSS 样式更新
- 在 `<style>` 标签末尾添加用户提供的新 CSS 样式（Office/PPT 风格工具栏）
- 替换现有的文本工具栏相关样式（496-583 行）

## 2. HTML 结构修改
- 替换现有的 `#comp-props-text` 区域（705-760 行）为用户提供的新 HTML 结构
- 新布局：字体 -> 字号 -> 样式(B/I/U/S) -> 颜色 -> 对齐 -> 行高

## 3. JavaScript 函数更新
- 替换现有的 `syncTextToolbar` 函数（918-949 行）为用户提供的新函数
- 实现样式回显功能，包括字号、字体、颜色、按钮状态、对齐和行高

## 4. 预期效果
- 底部面板会自动变成一排整齐的工具栏
- 可以像用 Word 一样直接修改字体、颜色、加粗等
- 点击颜色按钮时，下方的小色条会实时变色
- 选中文字时，工具栏会自动回显当前样式