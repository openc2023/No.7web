# 升级左侧Structure为专业节点树编辑器

## 1. 替换CSS样式
- 找到并替换原有CSS中的`/* 3.2 左侧：菜单树样式 */`部分
- 添加新的专业大纲树样式，包括层级线、折叠箭头动画和高亮选中态

## 2. 修改HTML结构
- 找到`<aside class="sidebar sidebar-left">`元素
- 更新其内容，将ID改为`structure-root`
- 替换原有的菜单容器为新的嵌套列表结构

## 3. 重写JavaScript逻辑
- 替换原有的菜单处理逻辑，包括`loadMenu`、`renderMenuTree`、`initSortable`等函数
- 实现新的嵌套拖拽逻辑
- 添加递归生成HTML的功能
- 实现从DOM反解数据的功能
- 添加节点选择、折叠/展开、添加子节点等交互功能

## 4. 集成到现有系统
- 确保新的节点树编辑器与现有GrapesJS编辑器集成
- 确保数据同步和保存功能正常工作

## 5. 测试功能
- 测试节点的拖拽和嵌套
- 测试折叠/展开功能
- 测试添加、删除和编辑节点功能
- 测试数据保存和加载功能