## 1. 升级 SDK 核心 (admin/core/Studio.php)
- 添加 `checkAndInitProject` 方法，实现自动初始化项目功能
- 修改 `__construct` 方法，确保必要的文件夹存在
- 更新 `getMenu` 方法，调用 `checkAndInitProject` 进行自动初始化
- 添加 `initDefaultProject` 私有方法，创建默认项目结构
- 更新 `getPage` 方法，确保与新的初始化逻辑兼容

## 2. 更新 API 接口 (admin/api/get-menu.php)
- 确保 API 正确调用 SDK 的 `getMenu` 方法
- 简化 API 逻辑，让 SDK 内部处理初始化

## 3. 前端自动加载 (admin/index.php)
- 修改 `loadMenu` 函数，在获取菜单后自动加载第一个页面
- 实现 UI 选中效果和内容加载

## 4. 预期效果
- 当用户第一次打开编辑器时，后台会自动创建 `site.json`、`index.html` 和 `works.html`
- 左侧会自动出现"首页"和"作品"菜单项
- 中间画布会自动加载首页内容
- 点击左侧的"作品"，画布会显示默认文字

## 5. 测试验证
- 删除 `/config/site.json` 文件
- 删除 `/pages-source/index.html` 和 `/pages-source/works.html`
- 刷新编辑器，观察自动初始化效果