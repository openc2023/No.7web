# 修复拖拽问题并实现矩阵吸附功能

## 1. 替换JavaScript部分
- 保留原有的StyleSystem和菜单相关代码
- 修改初始化顺序，先初始化编辑器，再加载菜单和组件
- 实现loadComponentsToBlocks函数，将组件加载到BlockManager
- 重写initGrapesJS函数，配置BlockManager和自定义渲染
- 删除原有的组件加载和渲染相关函数

## 2. 更新CSS样式
- 确保comp-grid样式正确，支持组件换行显示
- 确保comp-card样式正确，一行显示两个组件
- 实现矩阵容器的样式，支持自动吸附功能

## 3. 验证测试
- 测试组件能否正确加载到BlockManager
- 测试拖拽组件到画布是否正常工作
- 测试矩阵容器的自动吸附功能
- 测试点击组件添加到画布是否正常工作

## 技术说明
- 使用GrapesJS原生的BlockManager机制，解决原生拖拽在iframe中失效的问题
- 利用CSS Grid实现矩阵吸附功能，自动将组件吸附到格子里
- 自定义BlockManager渲染，保持原有的UI设计
- 支持点击添加和拖拽添加两种方式

## 预期结果
- 组件能正确加载并按分类显示
- 拖拽组件到画布能正常工作，支持跨iframe拖拽
- 矩阵容器能自动吸附组件，实现网格布局
- 点击组件能直接添加到画布
- 保持原有的UI设计风格