## 计划：实现底部属性栏控制、存储功能和视觉优化

### 第一阶段：打通“控制台”
1. 更新 `StyleSystem` 逻辑，使其能真正控制画布里的组件
2. 替换 `updateStyle` 函数为真实的 GrapesJS API 调用
3. 实现 `handleStyleChange` 函数，支持全局和局部样式修改
4. 修改底部栏相关的交互逻辑，使其与 GrapesJS API 绑定

### 第二阶段：打通“存储”
1. 创建保存接口 `admin/api/save-page.php`
2. 绑定保存按钮，实现 `saveProject` 函数
3. 修改 `initGrapesJS` 函数，实现自动加载功能，启动时读取保存的文件

### 第三阶段：视觉降噪
1. 强制画布暗黑模式
2. 美化选中框样式
3. 确保画布背景与后台一致
4. 添加 FontAwesome 支持到画布

### 实现步骤
1. 首先修改 `admin/index.php` 中的脚本部分，更新 `StyleSystem` 和相关函数
2. 创建 `admin/api/save-page.php` 保存接口
3. 修改保存按钮，添加 `onclick` 事件
4. 更新 `initGrapesJS` 函数，添加组件选中监听器和自动加载功能
5. 添加画布暗黑模式和样式优化
6. 测试所有功能是否正常工作

### 注意事项
- 不要创建新组件，也不要改 UI 布局
- 只做用户要求的三件事：连线、存盘、刷漆
- 确保代码与现有代码风格一致
- 测试功能是否正常工作